# terraform-aks-platform-modules
Modular Terraform for AKS platform foundations on Azure: cluster, Azure AD Workload Identity (passwordless auth), ACR integration with least-privilege AcrPull, and reusable environment patterns with remote state.
